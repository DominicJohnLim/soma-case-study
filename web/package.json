{
  "name": "provenance-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static dashboard for the provenance kernel: demo run, verification report, tamper scenarios, and the case-study write-up. All data is generated at build time by running the actual kernel.",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "build": "node build.ts"
  },
  "devDependencies": {
    "marked": "^15.0.0"
  }
}
